

initHomeInjections() {
  // // Register the HomeController with GetIt
  // getIt.registerFactory<HomeController>(() => HomeController(getIt()));
  // // Register the HomeRepository with GetIt
  // getIt.registerFactory<HomeRepository>(() => HomeRepository(getIt()));
  // // Register the HomeService with GetIt
  // getIt.registerFactory<HomeService>(() => HomeService(getIt()));
  // // Register the HomeDatasource with GetIt
  // getIt.registerFactory<HomeDatasource>(() => HomeDatasource(getIt()));
}